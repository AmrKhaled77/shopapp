import 'package:flutter/cupertino.dart';

class FavoriteSceen extends StatelessWidget {
  const FavoriteSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('FavoriteSceen'),);
  }
}
