import 'package:flutter/cupertino.dart';

class ItemDeatilsScreen extends StatelessWidget {
  const ItemDeatilsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
